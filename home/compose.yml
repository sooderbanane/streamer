services:
  home:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
    ports:
      - 80:80
    networks: [homelab]

networks:
  homelab:
    external: true
